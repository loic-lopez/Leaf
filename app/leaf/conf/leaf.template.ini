[LeafServers]
servers_root_path = ${LEAF_SERVERS_ROOT_PATH}

[LeafConfiguration]
leaf_configuration_directory = ${LEAF_CONFIG_ROOT_PATH}
leaf_log_directory = ${LEAF_LOG_DIR_ROOT_PATH}

[HttpConfiguration]
mime_types_config_file = mime_types.ini